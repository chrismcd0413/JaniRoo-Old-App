import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jmcleanco.workapp',
  appName: 'JMCleanCo Work',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
